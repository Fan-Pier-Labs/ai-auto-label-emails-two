platform: "fargate"
app_name: "ai-email-labels"
aws:
  profile: "personal"
  region: "us-east-2"
public:
  domain: "ai-email-labels.fanpierlabs.com"

task:
  cpu: 1024
  memory: 2048
  spot: false
  replicas: 1
  ephemeral_storage: 21

# Change this to true after you set the name of this project. 
allow_create: true